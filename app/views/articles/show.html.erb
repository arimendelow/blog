<p>
	<strong>Title:</strong>
	<%= @article.title %>
</p>

<p>
	<strong>Text:</strong>
	<%= @article.text %>
</p>

<%= link_to 'Back', articles_path %>
<%= link_to 'Edit', edit_article_path(@article) %>

<h2>Comments</h2>
	<%= render @article.comments %> <!-- render comments according to the '_comments.html.erb' template -->

<h2>Add a comment:</h2>
<%= render 'comments/form' %>
